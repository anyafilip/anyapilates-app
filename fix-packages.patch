--- src/app/[locale]/(public)/page.tsx
+++ src/app/[locale]/(public)/page.tsx
@@ -43,6 +43,15 @@
     orderBy: { price: 'asc' }
   })
 
+  packages.sort((a, b) => {
+    const aIsIntro = a.name.toLowerCase().includes('intro')
+    const bIsIntro = b.name.toLowerCase().includes('intro')
+    if (aIsIntro && !bIsIntro) return -1
+    if (!aIsIntro && bIsIntro) return 1
+    return 0
+  })
+
   return (
--- src/app/[locale]/(public)/buy-credits/page.tsx
+++ src/app/[locale]/(public)/buy-credits/page.tsx
@@ -19,6 +19,13 @@
     orderBy: { price: 'asc' }
   })
 
+  packages.sort((a, b) => {
+    const aIsIntro = a.name.toLowerCase().includes('intro')
+    const bIsIntro = b.name.toLowerCase().includes('intro')
+    if (aIsIntro && !bIsIntro) return -1
+    if (!aIsIntro && bIsIntro) return 1
+    return 0
+  })
+
   return (
